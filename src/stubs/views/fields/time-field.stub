            <div>
                <x-input label="{{__('{{title}}')}}" name="{{column}}" type="time" wire:model="{{column}}"
                    placeholder="{{__('{{title}}')}}" />
            </div>
