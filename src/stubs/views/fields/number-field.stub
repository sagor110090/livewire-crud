
            <div>
                <x-input label="{{__('{{title}}')}}" name="{{column}}" type="{{type}}" wire:model="{{column}}"
                    placeholder="{{__('{{title}}')}}" />
            </div>
