
            <div>
                <x-input label="{{__('{{title}}')}}" name="{{column}}" type="number" wire:model="{{column}}"
                    placeholder="{{__('{{title}}')}}" />
            </div>
