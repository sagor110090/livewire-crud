            <div>
                <x-input label="{{__('{{title}}')}}" name="{{column}}" type="date"  wire:model="{{column}}"
                    placeholder="{{__('{{title}}')}}" />
            </div>
