

            <div>
                <x-input label="{{__('{{title}}')}}" name="{{column}}" type="text" wire:model="{{column}}"
                    placeholder="{{__('{{title}}')}}" />
            </div>
